# Microservices Standards

- Each service owns its data and API contract.
- Avoid shared database coupling.
- Document sync and async integration contracts.
- Every endpoint must define error behavior.
