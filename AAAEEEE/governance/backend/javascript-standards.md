# JavaScript Backend Standards

- Use small modules with explicit exports.
- Keep business logic separate from transport code.
- Use built-in assertions for demo tests.
- Handle invalid input before calling domain logic.
